#include "main.h"
/*


*/
typedef struct node {
	char *directory
	struct node *next;
}

struct node *path_to_directory
	struct node *head = NULL;
	struct node *tail = NULL;

char *read_line(input)  /** Getline, user input */

token = strtok(buffer, " ")
while (token != NULL)
	{

			
	}






